[package]
name = "iShell"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

[lib]
name = "ishell_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1.0", features = ["v4", "fast-rng", "macro-diagnostics"] }
tauri-plugin-shell = "2.3.3"
tauri-plugin-fs = "2.0.0"
tauri-plugin-dialog = "2"
tauri-plugin-os = "2"

# 🟢 [关键修改] sqlx：增加 bundled 特性
# 这确保在安卓编译时，SQLite 会被编译进二进制文件，而不是去寻找手机系统里不存在的 .so 库
sqlx = { version = "0.8", features = [ "sqlite", "runtime-tokio" ], default-features = false }
libsqlite3-sys = { version = "0.28", features = ["bundled"] }

# 🟢 [关键修改] russh：纯 Rust 实现，彻底摆脱 OpenSSL
russh = "0.40"
russh-keys = "0.40"

# 🟢 [关键修改] reqwest：强制使用 rustls
reqwest = { version = "0.11", default-features = false, features = ["json", "stream", "multipart", "rustls-tls"] }

tokio = { version = "1", features = ["full"] }
sysinfo = "0.30"
aes-gcm = "0.10"
pbkdf2 = "0.12"
hmac = "0.12"
sha2 = "0.10"
rand = "0.8"
base64 = "0.21"
chrono = { version = "0.4", features = ["serde"] }
zip = "0.6"
walkdir = "2"
regex = "1"
urlencoding = "2"
